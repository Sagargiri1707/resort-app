import React from 'react'

export default function Hero(props){
    return(
        <div className="section-title">
            <h4>{props.title}</h4>

            <div></div>        
    </div>
        )
}
